# Remix state / generated
.states/
src/.states/
artifacts/
src/artifacts/
typechain/
cache/
coverage/

# Build outputs (CI will generate)
build/
src/build/

# Node
node_modules/
npm-debug.log*

# Hardhat / local dev artifacts
deployments/*.json
hardhat-node.log
hardhat-node.pid

# Frontend generated contract artifacts
web/contracts/
src/web/contracts/

# Env
.env.states

# Supabase CLI temp files (created during db push/link)
/.deps/
/supabase/
